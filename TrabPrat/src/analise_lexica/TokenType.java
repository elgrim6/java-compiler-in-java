package analise_lexica;

public enum TokenType {
    INT, CHAR, BYTE, BOOLEAN, LONG, SHORT, FLOAT, DOUBLE,
    LBRACKET, RBRACKET, LBRACE, RBRACE, COMMA, SEMICOLON, ASSIGN, PR_NEW,
    ID, NUMERO, EOF
}

